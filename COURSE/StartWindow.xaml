<Window x:Class="COURSE.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:COURSE"
        mc:Ignorable="d"
        Height="500" Width="300"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="DeepPink" Padding="5,5,5,5" CornerRadius="10">
        <Border Background="White" CornerRadius="10" MouseDown="Border_MouseDown">
            <Grid>
                <Button Content="X" Click="Exit_Button_Click" Margin="240,0,0,440" Style="{StaticResource exitButton}"/>
                <StackPanel VerticalAlignment="Center">
                    <Image Source="Images/click.png" Width="150" Height="150"/>
                    <Button Content="Subject" Click="Subject_Button_Click" Style="{StaticResource signinButton}" Margin="0,25,0,0"/>
                    <Button Content="Major" Click="Major_Button_Click" Style="{StaticResource signinButton}" Margin="0,25,0,0"/>
                    <Button Content="Semester" Click="Semester_Button_Click" Style="{StaticResource signinButton}" Margin="0,25,0,0"/>
                    </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>